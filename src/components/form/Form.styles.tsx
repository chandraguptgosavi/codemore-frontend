import Unit from "constants/units";
import styled from "styled-components";

const StyledForm = styled.form`
  width: 100%;
  margin: ${Unit.rem.MD} 0;
  display: flex;
  flex-direction: column;
  align-items: center;
`;

export default StyledForm;
