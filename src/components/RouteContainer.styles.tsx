import styled from "styled-components";

const StyledRouteContainer = styled.div`
  width: 100%;
  flex: 1 1 auto;
  display: flex;
  justify-content: center;
`;

export default StyledRouteContainer;
